name: CI NetSpy 🕵️‍♂️ (Debug Mode)

on:
  workflow_dispatch: # Botón manual
  push:
    branches: [ "main" ]

jobs:
  debug-explorer:
    runs-on: ubuntu-latest
    # ALERTA: He quitado el "defaults: working-directory" para que no crashee
    
    steps:
    - name: 📥 Descargar código
      uses: actions/checkout@v4

    - name: 🕵️‍♂️ LA HORA DE LA VERDAD (Listar todo)
      run: |
        echo "📂 Estoy en la raiz del runner:"
        pwd
        echo "🌳 Árbol de directorios completo:"
        ls -R
        # Este comando nos mostrará CADA carpeta que existe en tu repo.

    - name: 🐹 Intentar compilar (Manually cd)
      # Intentamos entrar manualmente para ver si falla aquí
      run: |
        echo "🐢 Intentando entrar a c/netspy..."
        cd c/netspy || echo "❌ NO EXISTE c/netspy"
        cd ../..
        
        echo "🐢 Intentando entrar a cmd/netspy..."
        cd cmd/netspy || echo "❌ NO EXISTE cmd/netspy"
        
        echo "🐢 Intentando entrar a netspy (en raiz)..."
        cd netspy || echo "❌ NO EXISTE netspy en raiz"
